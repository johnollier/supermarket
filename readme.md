## Supermarket checkout

We now need a database. You should install sqlite and the ruby driver for it.

    brew install sqlite

    gem install sqlite3

### Rspec

    gem install Rspec

    rspec --init  

    
